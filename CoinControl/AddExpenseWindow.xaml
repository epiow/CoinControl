<Window x:Class="CoinControl.AddExpenseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoinControl"
        mc:Ignorable="d"
        Title="AddExpenseWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Description:" />
        <TextBox x:Name="DescriptionTextBox" Grid.Row="1" Margin="0,0,0,10" />

        <Label Grid.Row="2" Content="Amount:" />
        <TextBox x:Name="AmountTextBox" Grid.Row="3" Margin="0,0,0,10" />

        <Label Grid.Row="4" Content="Date:" />
        <DatePicker x:Name="DatePicker" Grid.Row="5" Margin="0,0,0,10" />

        <Label Grid.Row="6" Content="Note:" />
        <TextBox x:Name="NoteBox" Grid.Row="7" Margin="0,0,0,10" />

        <Label Grid.Row="8" Content="Type:" />
        <TextBox x:Name="TypeBox" Grid.Row="10" Margin="10,28,-10,-18" />

        <Label Grid.Row="10" Content="Payment Method:" />
        <TextBox x:Name="PaymentMethodBox" Grid.Row="10" Margin="0,59,0,-49" />

    </Grid>
</Window>
