<Window x:Class="CoinControl.addToBalance"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoinControl"
        mc:Ignorable="d"
        Title="addToBalance" 
        Height="443" 
        Width="400" 
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True">

    <Grid>
        <Label Content="Amount to Add:" 
               Margin="10,73,211,322" 
               Height="32.04" 
               FontFamily="pack://application:,,,/fonts/#Montserrat" RenderTransformOrigin="0.508,1.281"/>
        <TextBox x:Name="AmountTextBox" 
                 Margin="21,110,21,285" 
                 Height="32.04" 
                 FontFamily="pack://application:,,,/fonts/#Montserrat"/>
        <Button Click="Confirm_Btn" 
                Background="Green" 
                Margin="255,335,10,10" 
                Height="39.04">
            <TextBlock Text="Confirm" 
                       Foreground="White" 
                       Margin="10,0" 
                       FontWeight="Bold"
                       FontFamily="pack://application:,,,/fonts/#Montserrat"/>
        </Button>
    </Grid>
</Window>
